create table books(
    bid BIGINT,
    bauther varchar(20),
    btitle varchar(20),
    category varchar(20),
    isbn varchar(20),
    no_of_pages BIGINT,
    price BIGINT,
    publisher varchar(20),
    relase_date DATE,
    year bigint
);